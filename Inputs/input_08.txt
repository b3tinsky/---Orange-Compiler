program test_08
vars 
    int a, b, c;

func int sum(){
    vars int a, b, c;
    # a = 2; # Still can't handle constant variables
    # b = 3; # Still can't handle constant variables
    return a + b;
}

main()
{
    vars
        float a, b, c;
    # a = 1; # Still can't handle constant variables
    # b = 2; # Still can't handle constant variables
    c = a + b;

    # print("Your result: ", c); # Still can't handle prints
}
