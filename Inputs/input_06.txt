program test_06
vars 
    int a, b, c, d;
    int i;
    float x, y, z;

func void sum(){
    vars
        int sum_a, sum_b;
        int result;
    # print("sum"); # Still can't handle prints
}

main()
{
    vars
        int test;

    # a = 1; # Still can't handle constant variables
    # b = 2; # Still can't handle constant variables
    c = a + b;
    # input(d); # Still can't handle inputs

    # print("Your result: ", c + d); # Still can't handle prints
}
